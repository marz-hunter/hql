{
    "variables": {
        "var0": "__ARGV2__"
    },
    "query": "query ($var0:String!) {
		automation {
			_id,
			active,
			config,
			created_at,
			events,
			id,
			lambda_arn,
			title,
			trigger_type,
			updated_at,
		}
	}"
}