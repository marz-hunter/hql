{
    "variables": {
        "var0": "__ARGV2__"
    },
    "query": "query ($var0:String!) {
		labelcategoryconnection {
			edges {
				node {
					_id,
					description,
					id,
					labels(first:5) {
						edges {
							node {
								_id,
								id,
								name,
							},
						},
						nodes {
							_id,
							id,
							name,
						},
						total_count,
					},
					name,
				},
			},
			nodes {
				_id,
				description,
				id,
				labels(first:5) {
					edges {
						node {
							_id,
							id,
							name,
						},
					},
					nodes {
						_id,
						id,
						name,
					},
					total_count,
				},
				name,
			},
			total_count,
		}
	}"
}