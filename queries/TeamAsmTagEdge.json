{
    "variables": {
        "var0": "__ARGV2__"
    },
    "query": "query ($var0:String!) {
		teamasmtagedge {
			node {
				_id,
				category_name,
				has_scope,
				id,
				tag_name,
			},
		}
	}"
}