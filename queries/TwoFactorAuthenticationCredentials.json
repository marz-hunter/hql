{
    "variables": {
        "var0": "__ARGV2__"
    },
    "query": "query ($var0:String!) {
		twofactorauthenticationcredentials {
			backup_codes,
			id,
			qr_code,
			signature,
			totp_secret,
		}
	}"
}