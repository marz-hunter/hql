{
    "variables": {
        "var0": "__ARGV2__"
    },
    "query": "query ($var0:String!) {
		surveyanswerstatistics {
			survey_structured_response {
				_id,
				helper_text,
				id,
				reason,
				survey {
					category,
					id,
					question,
					question_settings,
					structured_responses(first:5) {
						edges {
							node {
								_id,
								helper_text,
								id,
								reason,
							},
						},
						nodes {
							_id,
							helper_text,
							id,
							reason,
							survey {
								category,
								id,
								question,
								question_settings,
								url,
							},
						},
						total_count,
					},
					url,
				},
			},
			total_count,
		}
	}"
}