{
    "variables": {
        "var0": "__ARGV2__"
    },
    "query": "query ($var0:String!) {
		emailforwardingsetting {
			_id,
			email_forwarding_address,
			id,
			security_email_forwardings(first:5) {
				edges {
					node {
						_id,
						id,
						security_email,
						status,
					},
				},
				nodes {
					_id,
					id,
					security_email,
					status,
				},
				total_count,
			},
		}
	}"
}