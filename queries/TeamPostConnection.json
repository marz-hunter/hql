{
    "variables": {
        "var0": "__ARGV2__"
    },
    "query": "query ($var0:String!) {
		teampostconnection {
			edges {
				node {
					created_at,
					id,
					message,
					title,
				},
			},
			nodes {
				created_at,
				id,
				message,
				title,
			},
			total_count,
		}
	}"
}