{
    "variables": {
        "var0": "__ARGV2__"
    },
    "query": "query ($var0:String!) {
		solutioninstance {
			_id,
			created,
			description,
			edit_url,
			enabled,
			id,
			linkable,
			name,
			solution {
				_id,
				custom_fields,
				description,
				id,
				name,
				test_button_enabled,
				title,
				url,
			},
			url,
		}
	}"
}