{
    "variables": {
        "var0": "__ARGV2__"
    },
    "query": "query ($var0:String!) {
		importassetscsvmutationpayload {
			asset_import {
				_id,
				archived_ids,
				duplicate_scanner_asset_submissions,
				id,
				import_errors,
				processed_existing_ids,
				processed_new_ids,
				state,
				valid_scanner_asset_submissions,
			},
			clientMutationId,
			errors(first:5) {
				edges {
					node {
						error_code,
						field,
						id,
						message,
						type,
					},
				},
				nodes {
					error_code,
					field,
					id,
					message,
					type,
				},
				total_count,
			},
			was_successful,
		}
	}"
}