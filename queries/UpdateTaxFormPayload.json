{
    "variables": {
        "var0": "__ARGV2__"
    },
    "query": "query ($var0:String!) {
		updatetaxformpayload {
			clientMutationId,
			errors(first:5) {
				edges {
					node {
						error_code,
						field,
						id,
						message,
						type,
					},
				},
				nodes {
					error_code,
					field,
					id,
					message,
					type,
				},
				total_count,
			},
			tax_form {
				created_at,
				hello_sign_client_id,
				id,
				signed_at,
				status,
				type,
				url,
			},
			was_successful,
		}
	}"
}