{
    "variables": {
        "var0": "__ARGV2__"
    },
    "query": "query ($var0:String!) {
		labelcategory {
			_id,
			description,
			id,
			labels(first:5) {
				edges {
					node {
						_id,
						id,
						name,
					},
				},
				nodes {
					_id,
					id,
					name,
				},
				total_count,
			},
			name,
		}
	}"
}