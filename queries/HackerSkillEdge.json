{
    "variables": {
        "var0": "__ARGV2__"
    },
    "query": "query ($var0:String!) {
		hackerskilledge {
			node {
				_id,
				id,
				proficiency,
				skill {
					_id,
					approved,
					description,
					id,
					name,
					status,
				},
			},
		}
	}"
}